# -*- coding: utf-8 -*-
"""my project1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1iIuWQRahlxjGsf6uFAgjqP3RKaM4shEk

import the requred lib to use its functionality
"""

import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
import seaborn as sns

from sklearn.model_selection import train_test_split,GridSearchCV,RandomizedSearchCV
from sklearn.metrics import mean_squared_error,mean_absolute_error,r2_score

"""# 1 Problem Statment:
Build a machine learning model to predict premium of a health insurance(Regression Problem)

**Independent variables:**
1. Age
2. Gender
3. Height
4. Weight
5. Health Insurance Cover

**Dependent Variable:**
Premium

**2 Data Gathering:**
"""

df=pd.read_excel('22 height_weight_data.xlsx')
df

"""**3 Explorstory Data Analysis:**

1 Checking Missing values:
"""

df.isna().sum()

"""we have the missing values in two independent variables i.e. Height(cm) and Weight(kg),in Heigth we have 7 missing values and in Weigh we have 6 missing values.we can impute these values with imputation techniques like mean or median.

2 Cheking for object column in dataset to encode:
"""

df.info()

"""we are having Name and Gender column in object form,we will encode only Gender column.

3 Detecting outliers in the dataset:
"""

sns.boxplot(x=df['Age(yrs)'])

"""In Age(yrs) there is no presence of outliers."""

sns.boxplot(x=df['Height (cm)'])

"""we are having outliers in 'Height (cm)',we will handel this outliers in feature engineering"""

sns.boxplot(x=df['Weight(kg)'])

"""we are having outliers in 'Weight(kg)',we will handel this outliers in feature engineering"""

sns.boxplot(x=df['Health Insurance cover'])

"""In Health Insurance cover there are no outliers

**4 feature Engineering:**

1 filing missing/null values:
"""

df.info()

df['Height (cm)'].isna().sum()

df['Height (cm)'].median()

df['Height (cm)'].fillna(df['Height (cm)'].median(),inplace=True)

df['Height (cm)'].isna().sum()

df['Weight(kg)'].isna().sum()

df['Weight(kg)'].median()

df['Weight(kg)'].fillna(65.0,inplace=True)

df['Weight(kg)'].isna().sum()

df.isna().sum()

"""Performing encoding over df['Gender'] feature:"""

df['Gender'].unique()

df['Gender'].replace({'male':0, 'female':1},inplace=True)

df.info()

"""Calculating the BMI with the help of Height and Weight"""

df['BMI']=df['Weight(kg)']/((df['Height (cm)']/100)**2)
df

sns.boxplot(x=df['BMI'])

df.drop(['Sr.no','Name','Height (cm)', 'Weight(kg)'],axis=1,inplace=True)

df

"""**5. Feature Selection:**"""

df.corr()

sns.heatmap(df.corr(),annot=True)

sns.pairplot(df)

"""**6.Model Building:**"""

x=df.drop('Premium',axis=1)
y=df['Premium']

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.3,random_state=1)

"""**1. Linear Regression**"""

from sklearn.linear_model import LinearRegression

linear_model=LinearRegression()
linear_model.fit(x_train,y_train)

"""7. Model Evaluation:"""

y_pred=linear_model.predict(x_test)

"""testing accuracy:"""

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train=linear_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""**2 Decision Tree:**"""

x_train, x_test, y_train, y_test = train_test_split(x,y, test_size=0.3, random_state=1)

from sklearn.tree import DecisionTreeRegressor,plot_tree

dt_model=DecisionTreeRegressor(random_state=1)
dt_model.fit(x_train,y_train)

"""Model Evaluation:

testing accuracy:
"""

y_pred = dt_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""### training accuracy :"""

y_pred_train = dt_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""**Hyperparameter Tuning:**"""

df_model=DecisionTreeRegressor(random_state=1)

"""1 GirdSerchCV:"""

parameters = {'criterion' : ["squared_error","absolute_error"],
              'max_depth' : np.arange(3,8),
              'min_samples_split' : np.arange(2,8),
              'min_samples_leaf' : np.arange(1,5)}

gscv_model = GridSearchCV(dt_model, parameters, cv=5)
gscv_model.fit(x_train, y_train)

dt_hyp_model=DecisionTreeRegressor(max_depth=np.int64(7), min_samples_leaf=np.int64(1),
                      min_samples_split=np.int64(6), random_state=1)
dt_hyp_model.fit(x_train,y_train)

"""testing accuracy:"""

y_pred = dt_hyp_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train = dt_hyp_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""2 RandomizedSearchCV:"""

dt_model=DecisionTreeRegressor(random_state=1)

parameters = {'criterion' : ["squared_error","absolute_error"],
              'max_depth' : np.arange(3,8),
              'min_samples_split' : np.arange(2,8),
              'min_samples_leaf' : np.arange(1,5)}

rscv_model = RandomizedSearchCV(dt_model, parameters, cv=5)
rscv_model.fit(x_train, y_train)

dt_hyp_rs_model = DecisionTreeRegressor(max_depth=np.int64(6), min_samples_leaf=np.int64(3),
                      min_samples_split=np.int64(5), random_state=1)

dt_hyp_rs_model.fit(x_train, y_train)

"""testing accuracy:"""

y_pred = dt_hyp_rs_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_train_pred = dt_hyp_rs_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

plt.figure(figsize=(50,50))
plot_tree(dt_hyp_model,feature_names=x.columns,filled=True)

"""**3.Random Forest**"""

x_train, x_test, y_train, y_test = train_test_split(x,y, test_size=0.3, random_state=1)

from sklearn.ensemble import RandomForestRegressor

rf_model = RandomForestRegressor(random_state=10)
rf_model.fit(x_train, y_train)

"""Model Evaluation:

testing accuracy:
"""

y_pred = rf_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train = rf_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""**Hyperparameter Tuning:**

1 GSCV:
"""

rf_model=RandomForestRegressor(random_state=1)

parameter={'criterion':['squared_error','absolute_error'],
            'max_depth':np.arange(3,8),
            'min_samples_split':np.arange(2,8),
            'min_samples_leaf':np.arange(1,5),
            'max_features':['sqrt','log2']}

gscv_model=GridSearchCV(rf_model,parameter,cv=5)
gscv_model.fit(x_train,y_train)

rf_hyp_gs_model=RandomForestRegressor(max_depth=np.int64(7), max_features='sqrt',
                      min_samples_leaf=np.int64(1),
                      min_samples_split=np.int64(2), random_state=1)
rf_hyp_gs_model.fit(x_train,y_train)

y_pred = rf_hyp_gs_model.predict(x_test)

"""testing accuracy:"""

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train = rf_hyp_gs_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""2 RSCV:"""

rf_model=RandomForestRegressor(random_state=1)

parameters = {'criterion' : ["squared_error","absolute_error"],
              'max_depth' : np.arange(3,8),
              'min_samples_split' : np.arange(2,8),
              'min_samples_leaf' : np.arange(1,5)}

rscv_model = RandomizedSearchCV(rf_model, parameters, cv=5)
rscv_model.fit(x_train, y_train)

rf_hyp_rs_model = RandomForestRegressor(criterion='absolute_error', max_depth=np.int64(6),
                      min_samples_leaf=np.int64(1),
                      min_samples_split=np.int64(4), random_state=1)

rf_hyp_rs_model.fit(x_train, y_train)

"""testing accuracy:"""

y_pred = rf_hyp_rs_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train = rf_hyp_rs_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""**4.KNN**"""

from sklearn.neighbors import KNeighborsRegressor
from sklearn.preprocessing import StandardScaler,MinMaxScaler

"""scaling:
standardization
"""

std_scaler = StandardScaler()

array = std_scaler.fit_transform(x)
x_std = pd.DataFrame(array, columns=x.columns)
x_std

x_train, x_test, y_train, y_test = train_test_split(x_std, y, test_size=0.3, random_state=1)

knn_model = KNeighborsRegressor()
knn_model.fit(x_train, y_train)

"""testing accuracy:"""

y_pred = knn_model.predict(x_test)

mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_test, y_pred)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_test, y_pred)
print("R2 Score :", r2)

"""training accuracy:"""

y_pred_train = knn_model.predict(x_train)

mse = mean_squared_error(y_train, y_pred_train)
print("Mean Squared Error :",mse)

mae = mean_absolute_error(y_train, y_pred_train)
print("Mean Absolute Error :",mae)

r2 = r2_score(y_train, y_pred_train)
print("R2 Score :", r2)

"""testing model by taking data from user:"""

age = 30
gender = 'female'
health_insurance_cover = 5000000
height = 152
weight = 50

def gender_encoder(gender):
  if gender=='male':
    return 0
  else:
    return 1

gender_encoder('female')

def bmi_converter(ht, wt):
  bmi = wt/((ht/100)**2)
  return bmi

bmi_converter(167, 55)

df.info()

test_df=pd.DataFrame({'Age(yrs)':[age],
                      'Gender':[gender_encoder(gender)],
                      'Health Insurance cover':[health_insurance_cover],
                      'BMI':[bmi_converter(height,weight)]})

test_df

rf_hyp_gs_model.predict(test_df)